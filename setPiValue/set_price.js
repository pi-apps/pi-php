// set_price.js
const piPrice = 3141540; // Setting an arbitrary price for Pi (absurd)

function setPiPrice() {
    console.log(`Pi price is set to $${piPrice}, but this has no real effect on the market.`);
}

setPiPrice();
